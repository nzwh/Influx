import React from 'react';

const Background: React.FC = () => {
  return (
    <div id="bg" className="fixed top-0 left-0 z-[-3] w-screen h-screen bg-radial"></div>
  );
};

export default Background;