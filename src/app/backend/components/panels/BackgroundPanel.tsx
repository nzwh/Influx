import React from 'react';

const Background: React.FC = () => {
  return (
    <div id="bg" className="fixed top-0 left-0 z-[-3] w-screen h-screen bg-gradient-to-b from-[#f5f7fa] to-[#eef1f5]"></div>
  );
};

export default Background;